import React from "react";
import {
  ShieldCheck,
  AlertTriangle,
  UploadCloud,
  RefreshCw,
  Send,
  Trash2,
  Check,
  Settings,
  X,
  Lock,
  Sparkles,
  Bot,
  Heart,
  MessageCircle,
  Share2,
  Eye,
  Menu,
  PanelRight,
  PanelLeft,
} from "lucide-react";
import { FaDiscord, FaTelegramPlane, FaVk } from "react-icons/fa";

export {
  ShieldCheck,
  AlertTriangle,
  UploadCloud,
  RefreshCw,
  Send,
  Trash2,
  Check,
  Settings,
  X,
  Lock,
  Sparkles,
  Bot,
  Heart,
  MessageCircle,
  Share2,
  Eye,
  Menu,
  PanelRight,
  PanelLeft,
};

export const DiscordLogo = ({ className }: { className?: string }) => (
  <FaDiscord className={className} />
);
export const TelegramLogo = ({ className }: { className?: string }) => (
  <FaTelegramPlane className={className} />
);
export const VKLogo = ({ className }: { className?: string }) => (
  <FaVk className={className} />
);
